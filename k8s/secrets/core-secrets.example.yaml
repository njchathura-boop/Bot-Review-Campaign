# TEMPLATE ONLY. This file contains no usable credentials.
# Copy it to core-secrets.local.yaml, replace every REPLACE_* value, and apply the copy.
# The populated *.local.yaml file is ignored by Git and must never be committed.
apiVersion: v1
kind: Secret
metadata:
  name: detectra-storage
  namespace: bot-campaign
type: Opaque
stringData:
  # S3 and S3-compatible DVC remotes are supported. Example:
  # s3://team-bucket/detectra
  DVC_REMOTE_URL: REPLACE_WITH_DVC_REMOTE_URL
  AWS_ACCESS_KEY_ID: REPLACE_WITH_ACCESS_KEY_ID
  AWS_SECRET_ACCESS_KEY: REPLACE_WITH_SECRET_ACCESS_KEY
  AWS_DEFAULT_REGION: ap-south-1
---
apiVersion: v1
kind: Secret
metadata:
  name: detectra-grafana-admin
  namespace: bot-campaign
type: Opaque
stringData:
  admin-user: admin
  admin-password: REPLACE_WITH_STRONG_GRAFANA_PASSWORD

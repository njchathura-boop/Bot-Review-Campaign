# TEMPLATE ONLY. This file contains no usable credentials.
# Copy it to core-secrets.local.yaml, replace every REPLACE_* value, and apply the copy.
# The populated *.local.yaml file is ignored by Git and must never be committed.
apiVersion: v1
kind: Secret
metadata:
  name: detectra-storage
  namespace: bot-campaign
type: Opaque
stringData:
  DVC_REMOTE_URL: s3://bot-campaign-bucket
  #"C:/Users/njcha/Desktop/My Files/IITM/SEM3/MLOPS/bot-campaign-dvc-storage"
  DVC_ENDPOINT_URL: http://minio.bot-campaign.svc.cluster.local:9000
  DVC_REMOTE_NAME: kubernetes
  # S3-compatible credentials used by MinIO
  AWS_ACCESS_KEY_ID: minioadmin
  AWS_SECRET_ACCESS_KEY: minioadmin
  AWS_DEFAULT_REGION: us-east-1
---
apiVersion: v1
kind: Secret
metadata:
  name: detectra-minio
  namespace: bot-campaign
type: Opaque
stringData:
  MINIO_ROOT_USER: minioadmin
  MINIO_ROOT_PASSWORD: minioadmin
  MINIO_BUCKET: bot-campaign-bucket
---
apiVersion: v1
kind: Secret
metadata:
  name: detectra-grafana-admin
  namespace: bot-campaign
type: Opaque
stringData:
  admin-user: admin
  admin-password: admin
